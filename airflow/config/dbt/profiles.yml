sqlite:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: '/opt/airflow/data/stock_data.db' # Adjust the path to point to your SQLite database
      schema: main
      schema_directory: '/opt/airflow/data/'
      schemas_and_paths:
        main: '/opt/airflow/data/stock_data.db'
